#include<stdio.h>

int main()
{
    // Initialize variable
    int x, y, SOMA;

    scanf("%d", &x);
    scanf("%d", &y);

    // Calculation
    SOMA = x + y;

    printf("SOMA = %d\n", SOMA);

    return 0;
}