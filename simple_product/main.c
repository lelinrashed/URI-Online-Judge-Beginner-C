#include<stdio.h>

int main()
{
    // Initialize variable
    int x, y, PROD;

    scanf("%d", &x);
    scanf("%d", &y);

    PROD = x * y;

    // Output
    printf("PROD = %d\n", PROD);

    return 0;
}
