#include<stdio.h>

int main()
{
    // Initialize variable
    int A, B, X;

    // Get input from user
    scanf("%d", &A);
    scanf("%d", &B);

    X = A + B;

    // Output the value
    printf("X = %d\n", X);

}